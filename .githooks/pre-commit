#!/bin/sh
# Estilo antes del commit. Los tests van en el pre-push.

set -e

cd "$(git rev-parse --show-toplevel)"

./gradlew ktlintCheck detekt --console=plain || {
    echo "pre-commit: falla el estilo. Probá ./gradlew ktlintFormat"
    exit 1
}
